<div class="container">
  <div class="form-container">
    <div class="w-150 mx-auto shadow p-5 py-5 my-5 bg-body rounded">
      <h2>Add a job experience</h2>
      <%= render 'components/experience-form' %>
    </div>
     <%= link_to "Back to top", experiences_path, class: 'btn btn-secondary' %>
     <%= link_to "Back to profile", user_path(current_user.id), class: 'btn btn-secondary' %>
  </div>
</div>
